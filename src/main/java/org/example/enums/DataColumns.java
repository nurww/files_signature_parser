package org.example.enums;

public enum DataColumns {
  DIR("dir"),
  FILENAME("fileName"),
  FILEPATH("filePath"),
  RELPATH("relPath"),
  CODE("code"),
  SUBJECT("subject"),
  DATE("date");

  DataColumns(String name) {
  }
}
